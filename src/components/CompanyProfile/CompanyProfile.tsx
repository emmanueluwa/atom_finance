import { CompanyKeyMetrics } from "@/app/utils/company";
import React from "react";

interface Props {
  symbol: string;
}

const CompanyProfile = ({ symbol }: Props) => {
  return <div>CompanyProfile</div>;
};

export default CompanyProfile;
